# springboot-java-user-manager

testing some stuff

I want to study, so, this repository should cover the basics first.

```
1. create a crud for user
    1.1. create a model
    1.2. create a post controller to save the user
    1.3. create a get controller to get all users
    1.4. create a get controller to get the information from the specified user
    1.5. create a delete controller to delete the user based on the id
    1.6. create a delete controller to delete all users
    1.7. create a updatede controller to update the selected user


```
